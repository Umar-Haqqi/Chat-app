"use client"

import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
    palette: {
        mode: 'light',
        primary: {
            main: '#0D1117',
            light: 'white',
        },
        secondary: {
            main: '#f50057',
        }
    }
})